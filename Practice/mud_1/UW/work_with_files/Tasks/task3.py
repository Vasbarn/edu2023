"""
Условия:
    1) Вывести на экран данные полученные из файла второй задачи(
    экспортировав функцию из второго задания)

    2) Создать функцию get_symbols, которая должна принимать на вход значение с данными из файла первой задачи.
    Ко всем элементам, кроме итогов нужно применить встроенную функцию chr(x), где x - это наш элемент из таблицы.
    Получив символ по его номеру, мы записываем полученный символ вместо этого номера(вместо элемента).
    Полученный результат функция должна вернуть.

    3) Создать функцию aggregate_it, которая должна принимать на вход значение с данными из файла второй задачи.
    По каждой строке в файле сделать следующие преобразования, из 11 столбцов должно получиться 2.
    Где в первый столбец записывается текст, в который первая часть состоит из всех букв этой строки, и вторая часть
    из всех чисел. Все объединённое в одном слово.
    А второй столбец это "итого" оставшиеся без изменений
    Измененные данные должны быть возвращены этой функцией.

    4) Результат функции aggregate_it сохранить в отдельный файл "Матрица 3.txt"

    5) Вывести на экран значения Матрицы 3

    Сопроводить все действия логирование.(делать print с информативными данными о том,
    какой процесс сейчас происходит)

"""
# Твой код:



